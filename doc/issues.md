# 待决议题

本页只记录需要产品或维护者确认的选择。确认后将结论写回对应规范，并删除本页条目。Web 工程验证见 [web/issues.md](web/issues.md)。

## D4 规格与详细设计的分类

现有 `SPEC` 文件同时包含产品行为、目录划分、接口和数据库设计。此项暂缓，后续整理分类时再确定。

| 选项 | 组织方式 | 影响 |
|---|---|---|
| A（建议） | `SPEC/` 保留行为、约束与验收；新增同领域 `design/` 承载模块、接口实现、存储与部署设计 | 符合当前文档职责规范，需拆分 Web 与遥测文档并更新引用 |
| B | 明确将 `SPEC` 定义为实现规格，允许包含详细设计 | 路径不变，需要调整项目文档分类规则 |

本轮保持文件路径，已修正内容错误并将视觉规范与文档索引分开。分类确认后再统一迁移。
